# LiteAPI Landing

A minimal landing page that showcases the `liteapi-map-sdk` in the browser.

## What’s Inside

- Single-page landing built with Vite + TypeScript
- SDK wiring and usage example for `liteapi-map-sdk`
- Minimal UI suited for product or SDK presentations
